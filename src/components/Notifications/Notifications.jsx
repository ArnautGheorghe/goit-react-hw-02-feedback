import styles from "./Notifications.modules.css"
const Notifications = ({message}) => {
    
    return (
        <div>
            <h3>{message}</h3>
        </div>
    )
}

export default Notifications;